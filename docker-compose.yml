version: '3.8'

services:
  brochure-portal:
    build: .
    container_name: ingenero360_brochure_portal
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
    environment:
      - PORT=5000
      - FLASK_ENV=production
